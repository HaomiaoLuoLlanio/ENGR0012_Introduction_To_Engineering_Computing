
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

//My grade throughout this class does not reflect the amount of time and effort I put into all of the work I have done this semeester, its important that you know that, coding is by no menas something I am good at.
int main()
{
	FILE *festival.txt
	fopen(festival.txt, "w");
	fprintf(fp, "Current user: Santiago Ortega-Brown, SPO43");
	
	//I know there are also while loops which I couldve used but ive practiced mainly with if loops, and even then they dont work more than half the time.
	char p, P;
	char r,R;
	Thing = printf("Music Festival 2026. Enter your 4-digit password:");
	if (Thing != 5678)
	{
		printf("Incorrect password. Bye!");
	}
	else 
	{
		question = printf(" would you like to pay or check the ratings?: ");
		scanf(" %c", r, R, p, P);
		
		if (question != r, R, p, P)
		{
			question = printf(" would you like to pay or check the ratings?: ");
			scanf(" %c", r, R, p, P);
		}
	}
	switch
	{
		case 1 (question == "p" || "P")
		{
			int [] //This is uspposed to mean that any number can be placed within
			tickets = printf("Please enter a number of tickets to purchase: ");
			scanf(%d, []);
			
			if (tickets == 0)
			{
				tickets = printf("Please enter a number of tickets to purchase: ");
				scanf(%d, []);
			}
		}
		case 2 (question == "r" || "R") //Options were either || or &&, cant remeber, tests give brain fog
		{
			fopen(saturday.c, "r"); //"r" for read, "w" for write, "a" for append. I know that much
			float array[][];
			fclose(saturday.c);
		}
		//not gonna lie, C makes me miss Excel and MATLAB
	return 0;
}

void printf("Total payment is $%d.2", []);
//void commonly used to call functions
void fopen(festival.txt, "a");
fprintf(festival.txt, "The average rating for Shift A is ## and the average rating for Shift B is ##.");
//frankly "Shift A" isnt a terrible name for a band
fclose(festival.txt);
