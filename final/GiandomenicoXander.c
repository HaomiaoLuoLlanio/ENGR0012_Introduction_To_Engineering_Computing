#include <math.h>
#include <stdio.h>

int main()
{
FILE *fp;
int l
int p=5678;
int choice
int TOTAL
char choice = l
int amount
FILE *fp=festival.txt;
fprint("Current user: Xander Giandomenico, xjg2");

printf("\nMusic Festival 2026. Enter your 4-digit password: "); //asking for password
scanf(" %c", p);
if 
{
	p != 5678
	printf("Incorrect password. Bye!");
	else 
	{ 
	printf("\nPlease enter a P for payment or R for ratings", %c);
	if {
		{l != P || l != R};
		printf("please try again"); //making sure user did not enter anything but a P or R
	break;
	else 
	scanf( %c, choice);
	break;
	
}
		
		
	}
}

 switch choice //switch case for payment or ratings
 {
 case P
 {
	 printf("please enter the amount of tickets you would like to buy: ");
	 if  %d <= 0;
	 printf("please enter a valid amount");
	 else
	 scanf("%d", amount);
	 printf("\nTotal payment is $%lf", TOTAL);
 }
 
 
 case R
 {
	 printf("Please enter a file name: ", 'r');
	 fopen('r');
	 break
 }
fclose;
}
fopen("festival.txt"); //open festival file for ratings
printf("\nThe average rating for Shift A is %lf and the average rating for Shift B is %lf.", [0][1],[1][1]);
fclose("festival.txt");



	return 0;
}



//functions
function Payment =
{
(amount*33)+((1/5)*amount) = TOTAL;
}


function Ratings =
{
ArrayR = [i][j];
	if [i] < 7
	scanf("Shift A");
	else
	scanf("Shift B");
	printf("\nShift\tRatings");
	ShiftB = avg(Shift B);
	ArrayR;
	
	
}

//sorry you had to grade this
