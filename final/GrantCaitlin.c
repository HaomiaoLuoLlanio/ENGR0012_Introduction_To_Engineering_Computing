#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{

	FILE *fp //Creating and opening file festival.txt
	fp * fopen(festival.txt, "w");
	fprintf(festival.txt, "Current user: Caitlin Grant, cmg260");
	fclose;
	
	int password;
	char letter;
	int number; 
	
	printf("Music Festival 2026. Enter your 4 digit password:/n"); //ask the user to input their password
	scanf("%d", password);
	if (password != 5678)
	{
		printf("Incorrect password. Bye!"); //statement and end if correct password is not input
	}
	else
	{
		printf("Enter the letter P for payment to R for ratings"); //asking user to enter either P or R
		scanf("%c", letter);
		while (letter != P || R)
		{ printf("Not an option, enter a new letter"); //While the user inputs anthing other than P or R this message appears
		}
		
		switch (letter)
			{
				case "P" //case of user inputting P
				printf("Total payment is $$ %10.2lf", total); //total payment calculated within the function
				break;
				
				case "R" //case of user inputting R
				printf("Enter a filename for music festival data");
				scanf("%s", filename);
				while (filename == NULL)
				{
					printf("Error with file name entered");
				}
				FILE *fp
				fp * fopen("%s", saturday.txt);
				float arrR [][] (0,EOF);
				fclose;
				
				
				break;
				
	
	return 0;
}


	float Payment (int number) //creating the payment function.
	int total;
	total = (n*33)+(.20*n*33);
	return total;
	
	int Ratings ()
	char arr[][0];
	i = 0;
	if (arr[i][0] >=3 && < 7);
	{ 
		arr[i][0] = A;
		i++;
	}
	else if (arr[i][0] >=7);
	{
		arr[i][0] = B;
		i++;
	}
	
	printf("Shift /t Rating/n");
	printf("%f,/t%f",arr[][0], arr[][1]);
	
	return

	
	
	
	
	
