#include <stdio.h>

float Payment(int total, int tickets);
float Ratings(float times, int avga, int avgb);

int main(int argc, char **argv)
{
	FILE *fptr
	fopen("festival.txt");
	printf("Current user: Derek Corigliano, dec206");
	fclose("festival.txt");
	
	double password;
	printf("Music Festival 2026. Enter your 4-digit password: ");
	scanf(" %lf", &password);
	
	// This error checks password
	if(password != 5678){
		printf("Incorrect password. Bye!");
	}
	
	// This presents user with a choice P or R
	char choice;
	printf("Enter P for payment or R for ratings: ");
	scanf(" %c", &choice);
	
	while (1){
	if(choice != 'P' || choice != 'R')
	}
	
	// switch case for P or R
	switch
		case 'P'
		int tickets = 0;
		printf("Please enter the amount of festival tickets you would like to purchase: ")
		scanf("  %d", &tickets);
		while (1){
			if(tickets == 0);
		}
		float Payment (total, tickets);
		printf("Total payment is $%d10.2", total);
	
		case 'R'
		FILE *fptr = 'saturday.txt'
		float filename;
		float data[100][2] = 'saturday.txt'
		int rows = 0;
		
		printf("Enter the file name of you music festival data: ");
		scan(" %s", &filename);
		
		if (filename = 'saturday.txt'){
		fopen('saturday.txt');
		fscan(*fptr, "%d %d", data[rows][0], data[rows][1], != EOF) }
		rows++;
		
		float Ratings(times, avga, avgb);
		
		FILE *fptr
		fopen('festival.txt');
		printf("The average rating for Shift A is %d and the average rating for Shift B is %d");
		fclose(*fptr);
		
	
	return 0;
}
// This is Function 1
float Payment(int total, int tickets);
int total = 0;

total = tickets*33+(1/5)*(tickets*33)

// This is Function 2
float Ratings(float times, int avga, int avgb);
int avga = 0;
int avgb = 0;
float times[100][2];

times[0][0] = "Shift";
times[0][1] = "Rating";

while(1){
	if(i = 0; i<100; i++)
		if(j = 0; i<2; i++)
	times[i][j] = data[i][j];
}


