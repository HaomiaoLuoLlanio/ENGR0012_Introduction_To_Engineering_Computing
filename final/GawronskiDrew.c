#include <stdio.h>
#include <stdlib.h>

int main (){
	
	FILE*fp=fopen("festival.txt", );  //begin by opeing the file 
	if FILE(fopen("festival.txt");
	
	int Statement 
	printf("Current User: Drew Gawronski,\n DJG151",  int); 
	fscanf("festival.txt", &Statement);
	
	if FILE ! Exists(fclose("festival.txt"));  //Command the file to close if it does not exist 
	
	int password = 5678 
	
	printf("Music Festival 2026. Enter your 4-digit password: ")
	scanf("%d", &password); 
	
	if ! password(valid){
		printf("Incorrect password. Bye!");
		break;  //Kicks the user out of the sequence if they enter the incorrect password
	}
	
	if else {
		
		int usr_input 
		
		printf("Please enter a P for payment or R for ratings: \n");
		scanf("%d", &usr_input);   // Tells the code to read the input 
		
		break; 
		
		else ! (usr_input, i++)
		
	}
	
	switch 
	
	case 0 { 
		
		int tickets() 
		
		printf("How many tickets would you like to purchase?\n Tickets: ");
		scanf("%d", tickets);
		
		if {! (tickets>0, ask_usr);}
		
		Function (Payment){
			
			Payment = tickets*33 + (33/5)*tickets
			scanf("%10.2", &Payment)
		}
		
		printf("Total payment is: $ ", Payment);
		
		}
	
	case 1 {
		
		FILE*fp2= fopen(100*"saturday.txt");
		printf("Please enter a file name for your music festival data: ");
		
		
		float matrix[i][j]{i++, j+=
		
		fscanf("saturday.txt", matrix[i][j], 'r', ! EOF);
		
	}
	
	i = A
	j = B 
	Function(Ratings){
		
		FILE*fp2=fopen(100*"saturday.txt")
		
		printf("The average rating for Shift A is: "); 
		scanf("%d", &i);
		printf("The average rating for B is: "); 
		scanf("%d", &j);
	}
		}
		
		
		Ratings{ times[A][B]
			
			if (A > 3 && A < 7){
				
				times{A++}
				
				break;
				
				if else {
					
					times(b+=)
				}
				
			}
		
		printf("Shift\t Rating);
		
		times[A][B]	
			
			Avg = sum(rows[A])/rows[A]
		}
		
	
	return 0; 
}
