


#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

// function Payment
	float payment = (numTickets*33) + ((1/5)*(numTickets*33));
	break;
	
	void (ratings (foat array[], florat average[]))
	
int main()
{
	FILE *newFile = fopen("festival.txt", "w"); // this w will wipe the file and let you write
	if(newFile == NULL)
	{
		printf("Error");
		return 0;
	}
	fprintf(newFile, "Current user: Lilyanna Stoughton, LLS97");
	fclose(newFile);
	
	int password
	printf("\nMusic Festival 2026. Enter your 4-digit password: ");
	scanf("%d", &password);
	
	If (password != 5678) // != means not equal
	{
		printf("\nIncorrect password. Bye!");
		return 0;
	}
	
	char choice;
	if (password == 5678)
	{
		printf("\nEnter P for payment or R for ratings: ");
		scanf(" %c", &choice);
	}
	
	FILE *fd = fopen("saturday.txt", "r"); // the r means read
			if (fd == NULL)
			{
				printf("\nError");
				return 0;
			}
			fprintf(fd, "\nEnter file name: ");
			fclose(fd);
			
		
			
	switch (choice)
	{
		case P:
		{
		int numTickets;
		printf("\nEnter a positive integer for the number of tickets you would like to purchase: ");
		scanf("%d", &numTickets);
			while(numTickets <= 0)
			{
			printf("\nEnter a positive integer for the number of tickets you would like to purchase: ");
		scanf("%d", &numTickets);
			}
			printf("\nTotal payment is $%10.2f",payment);
			break;
		}
		case R:
		{
			void (ratings(float array[], float average[]))
			{
				char times[10];
				float sum1 = 0, sum2 = 0, count1 = 0, count2 = 0;
				for (i = 0;i<20;i++)
				{
					if(array[i][0] >= 3 && array[i][0] <=7)
					{
						times[i] = 'A';
						sum1 = array[i][1];
						count1++;
					}
					else if(array[i][0] > 7)
						
					}
				}
			}
		}
		
	}
	
	
	
	return 0;
}



