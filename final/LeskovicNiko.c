#include <stdlib.h>
#include <math.h>
#include <stdio.h>

int main()
{
	
	int password;
	int tickets;
	
	fopen = *fp;
	
	printf("Current user: %s, %s", Name, Userid);
	
	printf("Music Festival 2026. Enter your 4-digit password: ");  // Asks the user for the password
	scanf("%d", password);
	if (password != 5678)
	{
		printf("Incorrect password. Bye!"); // If the password is wrong it displays this message
		
	} 
	else {
		
		printf("Enter the letter P for payment, or R for ratings: ");
		scanf("%c", input);
		
		
		if (input != 'P' || input != 'R')
		{
		printf("Enter the letter P for payment, or R for ratings: ");
		scanf("%c", input);
	}
	
	switch (input)
	
	case 'P'
	{
		printf("How many tickets would you like to purchase?");
		scanf("%d", tickets);
		
		if (tickets <= 0)
		{
			printf("How many tickets would you like to purchase?");
		scanf("%d", tickets);
		
		
		break;
	}
	
		
	}
	case 'R'
	{
		printf("Enter a file name for your music festival data: ");
		scanf("%s", filename);
		fopen("filename");
		
		if (filename == NULL)
		{
			printf("Enter a file name for your music festival data: ");
		scanf("%s", filename);
		fopen("filename");
	}
	
	fopen("saturday.txt");
	float arr[][];
	fclose("saturday.txt");
	
	function(ratings)
	fopen("festival.txt");
	printf("The average rating for Shift A is %d, and the average rating for Shift B is %d.", ShiftA, &ShiftB);
	fclose("festival.txt");
	
	
			
		
		
	}
		
		
}
	return 0;
}

payment
{
int total = tickets*33+(1/5)*(tickets*33)

}

Ratings
{
	int i=0;
	int j=0;
char Times = arr[i][j];
when(i>0; i++);
{
if (i >= 3)
{
	i = 'A'
}
else
{
	i='B'
}
i>0; i++
}

}

char ShiftA = avg(A*i/i);  //average of shift a
char ShiftB= avg(B*i/i); // average of shift b



printf("Shift \t Rating")
printf("%c\t%s", Ratings, &Times);

