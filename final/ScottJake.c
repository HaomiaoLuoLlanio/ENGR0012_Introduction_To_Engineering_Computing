#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

 int Fun1()
 int Fun2()


int main();
{
	
	int R=1;
	int P=0;
	float password;
	char filename[100];
	int payment;
	int tickets = 0;
	int paymenttotal
	char filename2=== NULL
	
	FILE *fin = fopen("festival.txt", filename, 'w'); 	//Open new file
		fprintf(fin, "Current User: Jake Scott, JTS202"); //insert data
		
		fclose("festival.txt",filename); 
			
		printf:("Music Festival 2026. Enter your 4-digit password:");
		scanf("%lf", password);
		
		if (password == 5678)
		{
			printf("Enter the letter P for payment or the Letter R for ratings: ");
			scanf("%d %d", P, R);
			
			
			
			switch (payment=0) //switch case to determine if P or R was selected
			{
				case 1:
						printf("Enter an integer value, to correspond to the number of festival tickets you would like to purchase: ");
						scanf("%d", tickets);
						if (tickets < 0 )
						{
							printf("Try again.")
						}
				
					break;
				FILE *fout = fopen("saturday.txt", filename2, 'r'); 
					
				case 2:
					printf("Enter a file name for their music festival data");
					scanf(%s,fout);
					while (filename2 =! NULL);
					{
						printf("Try Again.");
					}
				FILE *appfin = fopen("festival.txt", filename, 'a');		//FILE appending at bottom of text
				fprint(appfin, "The average rating for Shift B is ##."); 	
				fclose("festival.txt");
					
			} 
			
			
			else
			{
				printf("Incorrect password Bye!"); //END OF CODE IF PASSWORD IS INCORRECT
				
				break;
			}
			
		}
		

		
	
		
		
	return 0;
}

// P function 
 int paymenttotal = (tickets*33 + (1/5)* (tickets *33))
	{
return paymentotal 
}
//R function
 char Times[100][2]
 char data [100]
 int rows[2]
 
 printf("Shift \t Rating");
 
 float avg[0] = (data[times]/count (a));
float avg[1] = (data[times]/count (b));
 










